# Copy this file to .env and adjust if your local MySQL differs from the
# defaults set up in BACKEND.md. .env is gitignored -- each teammate keeps
# their own.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=queuesmart
DB_PASSWORD=queuesmart_dev_pw
DB_NAME=queuesmart_dev
